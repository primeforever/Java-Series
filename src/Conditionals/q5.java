package Conditionals;

public class q5 {
    static void main() {
        int streakDays = 31;

        String status = (streakDays >= 30) ? "Consistent" : "Irregular";

        System.out.println(status);
    }
}
